# Gateway Service - Production Configuration (Azure)

Logging:
  LogLevel:
    Default: "Warning"
    Microsoft.AspNetCore: "Warning"
    Yarp: "Warning"

Gateway:
  RateLimiting:
    PermitLimit: 1000
    WindowSeconds: 60
