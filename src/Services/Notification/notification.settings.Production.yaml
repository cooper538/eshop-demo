# Notification Service - Production

AllowedHosts: "${ALLOWED_HOSTS:-notification-service.internal.*.azurecontainerapps.io,localhost}"

Logging:
  LogLevel:
    Default: "Warning"
    Microsoft.AspNetCore: "Warning"
