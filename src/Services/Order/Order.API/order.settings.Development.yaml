# Order Service - Development Configuration
# Overrides base order.settings.yaml for local development

Logging:
  LogLevel:
    Default: "Debug"
    Microsoft.AspNetCore: "Information"

Order:
  Database:
    CommandTimeoutSec: 60
    MaxRetryCount: 1
