# Order Service - Production

AllowedHosts: "${ALLOWED_HOSTS:-order-service.internal.*.azurecontainerapps.io,localhost}"

Logging:
  LogLevel:
    Default: "Warning"
    Microsoft.AspNetCore: "Warning"

Order:
  Database:
    CommandTimeoutSec: 30
    EnableRetry: true
    MaxRetryCount: 5
