# Order Service - Production Configuration
# Overrides base order.settings.yaml for production environment

Logging:
  LogLevel:
    Default: "Warning"
    Microsoft.AspNetCore: "Warning"

Order:
  Database:
    CommandTimeoutSec: 30
    EnableRetry: true
    MaxRetryCount: 5
